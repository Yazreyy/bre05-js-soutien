let arrays = [1,2,3,4,5]

function displayArray(arrays){
    arrays.forEach(element => console.log(element));
}

displayArray(arrays);

const tableau2 = [10, 20, 30, 40, 50];
const tableau3 = [5, 4, 3, 2, 1];
const tableau4 = [100, 200, 300, 400, 500];

displayArray(tableau2);
displayArray(tableau3);
displayArray(tableau4);

